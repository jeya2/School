# Copy this file to ".env" and put your real key in it.
#   PowerShell:  Copy-Item .env.example .env
#
# .env is gitignored â€” the key must never be committed.
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=paste-your-key-here

# Optional. Defaults to gemini-3.5-flash-lite: cheapest, and the largest
# free-tier daily allowance. Move up to gemini-3.6-flash if dictation into
# the admission form is not accurate enough â€” it costs more free-tier quota
# per day but handles spoken dates and Tamil names better.
# GEMINI_MODEL=gemini-3.6-flash
